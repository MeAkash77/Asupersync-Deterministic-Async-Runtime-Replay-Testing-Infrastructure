# ISO-022

## Change

Extracted `UnixDatagram::anonymous` in `src/net/unix/datagram.rs` and routed
`bind_abstract`, `unbound`, `pair`, and `from_std` through it.

## Equivalence Contract

- Inputs covered: all constructors that intentionally avoid filesystem cleanup
  tracking.
- Ordering preserved: unchanged; any nonblocking setup still runs before the
  shared constructor shell.
- Error semantics: unchanged; all fallible socket creation/configuration work
  remains at the original callsites.
- Observable side effects: unchanged.

## Fresh-Eyes Review

- Verified the helper is only used for no-path/no-cleanup cases.
- Verified bound constructors still use `from_bound_with`.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/unix/datagram.rs`


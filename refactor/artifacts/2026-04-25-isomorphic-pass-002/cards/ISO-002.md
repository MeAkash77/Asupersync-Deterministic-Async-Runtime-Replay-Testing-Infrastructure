## Change: collapse repeated wasm unsupported branches in `src/net/udp.rs`

### Equivalence contract
- Inputs covered: wasm-only branches in the public unsupported UDP methods and poll shims.
- Ordering preserved: yes; each branch still evaluates the same dropped arguments before building the same error.
- Tie-breaking: unchanged.
- Error semantics: unchanged; all paths still call `browser_udp_unsupported_result(...)` or `browser_udp_poll_unsupported(...)` with the same operation string.
- Laziness: unchanged.
- Short-circuit eval: unchanged.
- Floating-point: N/A.
- RNG / hash order: unchanged.
- Observable side-effects: unchanged; wasm branches still only synthesize the same unsupported errors.
- Type narrowing: unchanged.

### Verification
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings` ✓
- Fresh-eyes review: verified identical operation strings and dropped-argument coverage after macro expansion.

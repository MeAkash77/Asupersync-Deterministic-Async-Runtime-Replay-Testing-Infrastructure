# ISO-025

## Change

Extracted `would_block_errno` in `src/net/unix/datagram.rs` and reused it at
the Unix-errno receive/peek sites.

## Equivalence Contract

- Inputs covered: the `socket::recv` and `socket::recvmsg` errno branches in
  the datagram readiness/peek paths.
- Ordering preserved: unchanged.
- Error semantics: unchanged; the predicate recognizes the same two errno
  values and nothing else.
- Observable side effects: none.

## Fresh-Eyes Review

- Verified all converted sites previously used the same two-value predicate.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/unix/datagram.rs`


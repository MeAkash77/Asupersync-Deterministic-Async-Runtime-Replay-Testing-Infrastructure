## Change: finish residual wasm cleanup in `src/net/udp.rs`

### Equivalence contract
- Inputs covered: the remaining wasm-only unsupported branch in `recv_from`, the dead inner wasm branch nested inside non-wasm-only `poll_send_to`, and the file-level wasm-only `needless_return` suppression.
- Ordering preserved: yes.
- Tie-breaking: unchanged.
- Error semantics: unchanged; all wasm-only unsupported paths still construct the same errors with the same operation strings.
- Laziness: unchanged.
- Short-circuit eval: unchanged.
- Floating-point: N/A.
- RNG / hash order: unchanged.
- Observable side-effects: unchanged.
- Type narrowing: unchanged.

### Verification
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings` ✓
- Fresh-eyes review: verified the deleted inner wasm branch was unreachable by construction because `poll_send_to` itself is `#[cfg(not(target_arch = "wasm32"))]`.

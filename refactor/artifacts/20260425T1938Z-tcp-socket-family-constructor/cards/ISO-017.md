# ISO-017

## Change

Extracted `TcpSocket::with_family` in `src/net/tcp/socket.rs` and rewired
`new_v4` and `new_v6` to delegate to it.

## Equivalence Contract

- Inputs covered: the two public zero-argument constructors.
- Ordering preserved: unchanged; the same state fields are initialized in the
  same order.
- Error semantics: unchanged; both constructors still return `Ok(Self)`.
- Observable side effects: unchanged; no I/O or global state.

## Fresh-Eyes Review

- Verified the helper only centralizes default field values and does not change
  public signatures.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/tcp/socket.rs`


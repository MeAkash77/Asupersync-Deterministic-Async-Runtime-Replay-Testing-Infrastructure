# ISO-013

## Change

Extracted `unsupported_result()` in the `src/runtime/reactor/io_uring.rs`
fallback stub and routed all repeated unsupported returns through it.

## Equivalence Contract

- Inputs covered: all public and trait methods on the non-live `IoUringReactor`
  stub.
- Ordering preserved: unchanged; each method still exits immediately.
- Error semantics: unchanged; every path still delegates to the same
  `unsupported()` constructor and returns the same `Unsupported` error text.
- Observable side effects: none.

## Fresh-Eyes Review

- Verified `unsupported_result()` is only used in the fallback stub module.
- Verified `registration_count()` remains untouched.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Delta

- `src/runtime/reactor/io_uring.rs`: 10 insertions, 6 deletions

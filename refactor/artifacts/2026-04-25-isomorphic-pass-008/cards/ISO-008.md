## Change: collapse residual direct wasm unsupported-result branches in `src/net/tcp/stream.rs`

### Equivalence contract
- Inputs covered: wasm-only `connect`, `connect_socket_addr`, and `set_keepalive`.
- Ordering preserved: yes; the same arguments are dropped before the same `Unsupported` error is returned.
- Tie-breaking: unchanged.
- Error semantics: unchanged; all affected branches still synthesize the same unsupported error for the same operation string.
- Laziness: unchanged.
- Short-circuit eval: unchanged.
- Floating-point: N/A.
- RNG / hash order: unchanged.
- Observable side-effects: unchanged.
- Type narrowing: unchanged.

### Verification
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings` ✓
- Fresh-eyes review: confirmed the macro reused the exact prior operation strings and did not alter async control flow.

# ISO-014

## Change

Collapsed the repeated unsupported returns in the non-Windows
`IocpReactor` stub in `src/runtime/reactor/windows.rs` into a shared
`UNSUPPORTED_MESSAGE`, `unsupported()`, and `unsupported_result()`.

## Equivalence Contract

- Inputs covered: all methods exposed by the non-Windows `IocpReactor` stub.
- Ordering preserved: unchanged; each method still returns immediately.
- Error semantics: unchanged; each path still returns
  `io::ErrorKind::Unsupported` with the same exact message.
- Observable side effects: none.

## Fresh-Eyes Review

- Verified the helper stays confined to the `#[cfg(not(target_os = "windows"))]`
  stub module.
- Verified only duplicated inline error construction moved.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Delta

- `src/runtime/reactor/windows.rs`: 16 insertions, 24 deletions

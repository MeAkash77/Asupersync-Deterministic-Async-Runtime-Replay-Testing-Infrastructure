## Change

Extract `errno_poll_error()` in `src/net/unix/datagram.rs`.

### Equivalence Contract

- Inputs covered: errno-to-poll-error conversions in readiness and peek paths.
- Ordering preserved: yes; the helper only centralizes terminal error conversion.
- Error semantics: identical `io::Error::from_raw_os_error(errno as i32)` payload.
- Observable side-effects: unchanged.

### Verification

- Fresh-eyes review: the remaining direct `from_raw_os_error(errno as i32)` poll branches in this file were removed.
- `cargo fmt --check -- src/net/unix/datagram.rs`
- `git diff --check -- src/net/unix/datagram.rs`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_check_asupersync cargo check -p asupersync --lib`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_check_asupersync cargo check -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev`
  - blocked by unrelated pre-existing compile failures outside this diff

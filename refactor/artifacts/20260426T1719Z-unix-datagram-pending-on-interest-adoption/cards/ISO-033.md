## Change

Adopt `pending_on_interest()` across `src/net/unix/datagram.rs`.

### Equivalence Contract

- Inputs covered: all remaining read/write interest re-arm branches in the datagram async and readiness APIs.
- Ordering preserved: yes; registration still occurs before returning `Poll::Pending`.
- Error semantics: identical; registration failures still surface as `Poll::Ready(Err(err))`.
- Observable side-effects: unchanged interest direction and waker registration behavior.

### Verification

- Fresh-eyes review: converted branches still target the same `READABLE` or `WRITABLE` interest as before.
- `cargo fmt --check -- src/net/unix/datagram.rs`
- `git diff --check -- src/net/unix/datagram.rs`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_check_asupersync cargo check -p asupersync --lib`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_check_asupersync cargo check -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev`
  - blocked by unrelated pre-existing compile failures outside this diff

# ISO-020

## Change

Extracted `UnixListener::with_inner` in `src/net/unix/listener.rs` and routed
`bind_abstract`, `from_std`, and `from_bound_with` through it.

## Equivalence Contract

- Inputs covered: abstract, std-derived, and bound listener construction paths.
- Ordering preserved: unchanged; nonblocking setup and bound-socket finalization
  still happen before the shared constructor shell runs.
- Error semantics: unchanged; all fallible work remains at the original callsites.
- Observable side effects: unchanged.

## Fresh-Eyes Review

- Verified the helper does not absorb bind/finalize side effects.
- Verified path ownership and cleanup identity values remain callsite-specific.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/unix/listener.rs`


## Change

Extract `fallback_pending()` and `UnixStreamInner::pending_on_interest()` in `src/net/unix/split.rs`.

### Equivalence Contract

- Inputs covered: borrowed-half `WouldBlock` fallback branches and owned-half registration branches.
- Ordering preserved: yes; fallback wake still happens before returning `Poll::Pending`, and owned halves still register identical interest before pending.
- Error semantics: identical registration failure propagation for owned halves.
- Observable side-effects: unchanged wake, registration, and interest direction behavior.

### Verification

- Fresh-eyes review: borrowed halves still only fallback-rewake, owned halves still target the same `READABLE`/`WRITABLE` interests.
- `cargo fmt --check -- src/net/unix/stream.rs src/net/unix/split.rs`
- `git diff --check -- src/net/unix/stream.rs src/net/unix/split.rs`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_check_asupersync cargo check -p asupersync --lib`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_check_asupersync cargo check -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev`
  - blocked by unrelated pre-existing `dead_code` / unused-item failures outside this diff

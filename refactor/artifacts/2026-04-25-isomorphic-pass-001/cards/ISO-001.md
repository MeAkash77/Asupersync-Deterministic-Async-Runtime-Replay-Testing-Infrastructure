# Card ISO-001 — Collapse repeated wasm unsupported-feature compile guards in `src/lib.rs`


> Fill EVERY row before editing. "I don't know" → write a property test first.

## Equivalence contract

- **Inputs covered:**          `src/lib.rs` item-level `#[cfg]` gates for wasm32 + unsupported features: `cli`, `io-uring`, `tls`, `tls-native-roots`, `tls-webpki-roots`, `sqlite`, `postgres`, `mysql`, `kafka`.
- **Ordering preserved:**      Yes. Item expansion order stays source-order; each `compile_error!` is still gated by the same predicate and appears in the same block.
- **Tie-breaking:**            Unchanged/N/A. No runtime or selection logic changes.
- **Error semantics:**         Same compile-time failure messages under the same feature combinations.
- **Error vs panic:**          Unchanged. This surface emits compile errors, not panics.
- **Laziness:**                Unchanged. No runtime evaluation introduced.
- **Short-circuit eval:**      Unchanged. `cfg` predicate structure is identical per guard.
- **Floating-point:**          N/A.
- **Integer overflow:**        Unchanged/N/A.
- **RNG / hash order:**        Unchanged/N/A.
- **Side-effects (logs/metrics/spans/DB writes):** None before or after.
- **Side-effect cardinality:** Zero before and after.
- **Async / Future identity:** N/A. No async/runtime path touched.
- **Type narrowing:**          Unchanged. Macro only emits existing item forms.
- **Public API / ABI / FFI:**  Unchanged. No exported types/functions/macros added to the public API surface.
- **Compile-time guarantees:** Unchanged. Unsupported wasm feature combinations remain fail-closed with the same predicates and user-visible messages; only the source spelling changed to a single table-style macro.
- **React semantics:**         N/A.
- **Serialization:**           N/A.
- **Concurrency:**             Unchanged. No shared state or lock ordering touched.
- **Resource lifecycle:**      Unchanged/N/A.
- **Telemetry:**               None before or after.

## Predicted ΔLOC

- Before: 27 lines deleted from the repeated unsupported-feature guard block
- After:  16 lines added for the table macro form
- Δ:      -11 lines actual (`git diff --numstat -- src/lib.rs`)

## Verification (run after edit)

- [x] `rch exec -- cargo check -p asupersync --lib`
- [ ] `rch exec -- cargo check -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev`
  Blocked by pre-existing unrelated dead-code failures in `src/cx/cx.rs`, `src/http/h1/codec.rs`, `src/io/browser_stream.rs`, `src/net/tcp/*`, `src/net/udp.rs`, and `src/runtime/builder.rs`.
- [x] Representative wasm negative check still fails with the same unsupported-feature message for `--features wasm-browser-dev,cli`
- [x] source-line delta matches the simplification goal
- [x] no new `#[allow]` / `// eslint-disable` / `# noqa` / `# type: ignore`

# ISO-024

## Change

Extracted `connect_sock_addr` in `src/net/unix/stream.rs` and routed
`UnixStream::connect` and `UnixStream::connect_abstract` through it.

## Equivalence Contract

- Inputs covered: filesystem-path and abstract-namespace Unix stream connects.
- Ordering preserved: unchanged; socket creation, nonblocking setup, connect,
  and `wait_for_connect` still occur in the same order.
- Error semantics: unchanged; the same `SockAddr` construction and connect
  errors still propagate from the original callsites.
- Observable side effects: unchanged.

## Fresh-Eyes Review

- Verified only the shared connect pipeline moved; address encoding still
  happens at the original public entry points.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/unix/stream.rs`


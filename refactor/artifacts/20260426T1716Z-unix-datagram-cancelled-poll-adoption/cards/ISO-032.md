## Change

Adopt `cancelled_poll()` across `src/net/unix/datagram.rs`.

### Equivalence Contract

- Inputs covered: datagram async send/recv operations plus readiness and peek poll paths.
- Ordering preserved: yes; the helper only centralizes the terminal cancelled poll value.
- Error semantics: identical `io::ErrorKind::Interrupted` with the same `"cancelled"` message.
- Observable side-effects: unchanged; no new registration, probe, or wake behavior.

### Verification

- Fresh-eyes review: only helper definitions remain for direct cancelled poll construction in `src/net/unix/datagram.rs`.
- `cargo fmt --check -- src/net/unix/datagram.rs`
- `git diff --check -- src/net/unix/datagram.rs`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_check_asupersync cargo check -p asupersync --lib`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_check_asupersync cargo check -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev`
  - blocked by unrelated pre-existing compile failures outside this diff

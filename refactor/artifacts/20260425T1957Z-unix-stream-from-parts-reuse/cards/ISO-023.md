# ISO-023

## Change

Reused the existing `UnixStream::from_parts` helper in
`src/net/unix/stream.rs` for `connect`, `connect_abstract`, `pair`, and
`from_std`.

## Equivalence Contract

- Inputs covered: all touched stream constructors.
- Ordering preserved: unchanged; nonblocking setup and connect/wait logic still
  run before `from_parts`.
- Error semantics: unchanged; no fallible work moved behind the helper.
- Observable side effects: unchanged.

## Fresh-Eyes Review

- Verified `from_parts` already encoded the same lazy-registration invariant as
  the removed constructor shells.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/unix/stream.rs`


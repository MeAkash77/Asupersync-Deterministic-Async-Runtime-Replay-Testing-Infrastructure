## Change: collapse repeated wasm poll-unsupported shims in `src/net/tcp/split.rs`

### Equivalence contract
- Inputs covered: wasm-only poll shims for `ReadHalf`, `WriteHalf`, `OwnedReadHalf`, and `OwnedWriteHalf`.
- Ordering preserved: yes; each shim still drops the same arguments before returning the same `Unsupported` poll result.
- Tie-breaking: unchanged.
- Error semantics: unchanged; all affected branches still route through `browser_tcp_poll_unsupported(...)` with the same operation string.
- Laziness: unchanged.
- Short-circuit eval: unchanged.
- Floating-point: N/A.
- RNG / hash order: unchanged.
- Observable side-effects: unchanged.
- Type narrowing: unchanged.

### Verification
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings` ✓
- Fresh-eyes review: verified borrowed and owned split shims still use the same operation names and return type wrappers.

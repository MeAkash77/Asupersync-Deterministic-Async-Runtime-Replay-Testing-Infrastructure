## Change

Adopt `would_block_errno()` across `src/net/unix/datagram.rs`.

### Equivalence Contract

- Inputs covered: errno-based readable probes in the datagram readiness and peek paths.
- Ordering preserved: yes; only the boolean test moved into a helper.
- Error semantics: identical `EAGAIN` / `EWOULDBLOCK` handling and fallthrough behavior.
- Observable side-effects: unchanged; helper only centralizes the predicate.

### Verification

- Fresh-eyes review: the remaining raw `EAGAIN` / `EWOULDBLOCK` comparisons in this file are gone.
- `cargo fmt --check -- src/net/unix/datagram.rs`
- `git diff --check -- src/net/unix/datagram.rs`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_check_asupersync cargo check -p asupersync --lib`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_check_asupersync cargo check -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev`
  - blocked by unrelated pre-existing compile failures outside this diff

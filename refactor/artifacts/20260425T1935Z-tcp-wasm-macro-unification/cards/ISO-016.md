# ISO-016

## Change

Moved the duplicated wasm TCP unsupported-result and unsupported-poll helper
macros out of `src/net/tcp/stream.rs` and `src/net/tcp/split.rs` into
`src/net/tcp/mod.rs`, then rewired both files to use the shared parent-module
macros.

## Equivalence Contract

- Inputs covered: all wasm-only TCP result-returning and poll-returning
  unsupported branches in `stream.rs` and `split.rs`.
- Ordering preserved: unchanged; each branch still binds arguments and returns
  immediately.
- Error semantics: unchanged; every callsite still returns
  `super::browser_tcp_unsupported(<same op string>)`.
- Observable side effects: unchanged.

## Fresh-Eyes Review

- Searched for stale local helper references after the move; none remained.
- Checked that the poll macro only expands at callsites with `Poll` already in
  scope.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/tcp/mod.rs`
- `src/net/tcp/stream.rs`
- `src/net/tcp/split.rs`


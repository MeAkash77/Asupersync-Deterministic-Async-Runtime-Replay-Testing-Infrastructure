# ISO-012

## Change

Collapsed the remaining TCP wasm unsupported-result shims in
`src/net/tcp/socket.rs`, `src/net/tcp/listener.rs`, and
`src/net/tcp/traits.rs` behind one parent-module macro in
`src/net/tcp/mod.rs`.

## Equivalence Contract

- Inputs covered: wasm-only entry points for `TcpSocket::listen`,
  `TcpSocket::connect`, `TcpListener::bind`, and `TcpListenerBuilder::bind`.
- Ordering preserved: unchanged; each branch still immediately returns before
  any native side effects.
- Error semantics: unchanged; each path still returns
  `super::browser_tcp_unsupported(<same op string>)`.
- Laziness: unchanged; no new evaluation beyond tuple-binding the already-live
  arguments.
- Observable side effects: unchanged; the only effect remains construction of
  the same `Unsupported` `io::Error`.

## Fresh-Eyes Review

- Verified the macro resolves `super::browser_tcp_unsupported` at the
  invocation site in each sibling module.
- Verified no native code path changed.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Delta

- `src/net/tcp/mod.rs`: 8 insertions, 0 deletions
- `src/net/tcp/socket.rs`: 5 insertions, 6 deletions
- `src/net/tcp/listener.rs`: 4 insertions, 2 deletions
- `src/net/tcp/traits.rs`: 2 insertions, 2 deletions

# ISO-027

## Change

Extracted `pending_on_interest` in `src/net/unix/datagram.rs` and reused it for
the repeated would-block branches in send/recv/readiness/peek paths.

## Equivalence Contract

- Inputs covered: all touched would-block branches that previously registered
  readable or writable interest and then returned `Pending`.
- Ordering preserved: unchanged; `register_interest` still runs before the
  `Pending` return at each site.
- Error semantics: unchanged; any `register_interest` failure still becomes
  `Poll::Ready(Err(err))`.
- Observable side effects: unchanged.

## Fresh-Eyes Review

- Verified the helper only centralizes the existing post-would-block branch and
  does not move socket operations or cancellation checks.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/unix/datagram.rs`


# ISO-018

## Change

Extracted `TcpListener::accepted_connection` in `src/net/tcp/listener.rs` and
used it for both successful accept paths inside `poll_accept`.

## Equivalence Contract

- Inputs covered: both `Ok((stream, addr))` branches in `poll_accept`.
- Ordering preserved: unchanged; wakeup/reset logic still occurs before the
  stream conversion at each site.
- Error semantics: unchanged; `TcpStream::from_std` still governs conversion
  errors.
- Observable side effects: unchanged.

## Fresh-Eyes Review

- Verified the helper does not absorb any accept-storm bookkeeping or wakeup
  logic.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/tcp/listener.rs`


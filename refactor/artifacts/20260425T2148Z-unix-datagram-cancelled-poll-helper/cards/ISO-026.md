# ISO-026

## Change

Extracted `cancelled_poll` in `src/net/unix/datagram.rs` and reused it across
the datagram async/poll entry points.

## Equivalence Contract

- Inputs covered: all touched cancellation checkpoint branches in
  `UnixDatagram`.
- Ordering preserved: unchanged; the checkpoint still short-circuits before
  any socket operation.
- Error semantics: unchanged; each site still returns the same interrupted
  error kind and message.
- Observable side effects: unchanged.

## Fresh-Eyes Review

- Verified every converted branch returned the same exact error payload before
  the extraction.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/unix/datagram.rs`


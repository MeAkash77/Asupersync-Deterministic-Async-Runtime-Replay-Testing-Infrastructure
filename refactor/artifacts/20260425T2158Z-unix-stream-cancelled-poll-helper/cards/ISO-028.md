## Change

Extract `cancelled_poll()` in `src/net/unix/stream.rs`.

### Equivalence Contract

- Inputs covered: Unix stream connect wait loops, ancillary send/recv loops, and stream poll methods.
- Ordering preserved: yes; only the repeated terminal `Poll::Ready(Err(...))` constructor moved.
- Error semantics: identical `io::ErrorKind::Interrupted` with the same `"cancelled"` message.
- Observable side-effects: unchanged; no new wakeups, registrations, or I/O.

### Verification

- Fresh-eyes review: no divergent cancellation branches remained in `src/net/unix/stream.rs`.
- `cargo fmt --check -- src/net/unix/stream.rs src/net/unix/split.rs`
- `git diff --check -- src/net/unix/stream.rs src/net/unix/split.rs`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_check_asupersync cargo check -p asupersync --lib`
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_check_asupersync cargo check -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev`
  - blocked by unrelated pre-existing `dead_code` / unused-item failures outside this diff

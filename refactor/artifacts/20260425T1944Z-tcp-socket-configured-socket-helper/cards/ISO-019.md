# ISO-019

## Change

Extracted `TcpSocket::configured_socket` in `src/net/tcp/socket.rs` and routed
both `listen` and `connect` through it for shared family/domain/reuse setup.

## Equivalence Contract

- Inputs covered: native `listen` and native `connect`.
- Ordering preserved: unchanged; per-callsite bind/listen/connect steps remain
  in the original order after the shared setup.
- Error semantics: unchanged; the same `socket2` constructors and reuse option
  setters are called and propagate the same errors.
- Observable side effects: unchanged; no new socket operations were moved across
  callsite-specific bind/listen/connect steps.

## Fresh-Eyes Review

- Verified the helper does not absorb the `listen`-only backlog path or the
  `connect`-only optional local bind path.
- Verified platform gating remained `#[cfg(not(target_arch = "wasm32"))]`.

## Verification

- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings`: pass
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings`: pass

## Touched Files

- `src/net/tcp/socket.rs`


## Change: collapse repeated wasm unsupported-result branches in `src/net/tcp/stream.rs`

### Equivalence contract
- Inputs covered: wasm-only unsupported branches in the `TcpStream` result-returning methods.
- Ordering preserved: yes; each branch still drops the same arguments before returning the same unsupported error.
- Tie-breaking: unchanged.
- Error semantics: unchanged; all affected methods still call `browser_tcp_unsupported_result(...)` with the same operation string.
- Laziness: unchanged.
- Short-circuit eval: unchanged.
- Floating-point: N/A.
- RNG / hash order: unchanged.
- Observable side-effects: unchanged; wasm branches still only produce the same unsupported errors.
- Type narrowing: unchanged.

### Verification
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_fmt_asupersync cargo fmt --check --all` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_native_clippy_asupersync cargo clippy -p asupersync --lib -- -D warnings` ✓
- `rch exec -- env CARGO_TARGET_DIR=/tmp/rch_target_wasm_clippy_asupersync cargo clippy -p asupersync --lib --target wasm32-unknown-unknown --no-default-features --features wasm-browser-dev -- -D warnings` ✓
- Fresh-eyes review: confirmed the removed `needless_return` suppression was no longer needed after the macro collapse.
